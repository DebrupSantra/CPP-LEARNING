#include <iostream>
using namespace std;

// Example 9
int fib(int n) {
    if (n <= 1)
        return n;
    return fib(n - 1) + fib(n - 2);
}

int main() {

    // ====================================================
    // Example 1 : O(1)
    // ====================================================
    /*
    int n = 10;
    cout << n * 5 << endl;
    */

    // ====================================================
    // Example 2 : O(n)
    // ====================================================
    /*
    int n;
    cin >> n;

    for (int i = 0; i < n; i++) {
        cout << i << " ";
    }
    */

    // ====================================================
    // Example 3 : O(n²)
    // ====================================================
    /*
    int n;
    cin >> n;

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            cout << i << " " << j << endl;
        }
    }
    */

    // ====================================================
    // Example 4 : O(log n)
    // ====================================================
    /*
    int n;
    cin >> n;

    while (n > 1) {
        n /= 2;
    }
    */

    // ====================================================
    // Example 5 : O(n log n)
    // ====================================================
    /*
    int n;
    cin >> n;

    for (int i = 0; i < n; i++) {
        int x = n;

        while (x > 1) {
            x /= 2;
        }
    }
    */

    // ====================================================
    // Example 6 : O(n²)
    // ====================================================
    /*
    int n;
    cin >> n;

    for (int i = 0; i < n; i++) {
        for (int j = i; j < n; j++) {
            cout << i + j << " ";
        }
    }
    */

    // ====================================================
    // Example 7 : O(n²)
    // ====================================================
    /*
    int n;
    cin >> n;

    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= i; j++) {
            cout << "*";
        }
        cout << endl;
    }
    */

    // ====================================================
    // Example 8 : O(√n)

    return 0;
}